// A simple JavaScript function to greet a user.
function greet(name) {
  console.log("Hello, " + name + "!");
  return "Greeting complete.";
}

// Call the function and log its return value.
const result = greet("Anirudh");
console.log(result);