// C
#include <stdio.h>

int main() {
    int a, b;
    printf("Enter two numbers:\n");
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    if (scanf("%d %d", &a, &b) == 2) {
        printf("The sum is: %d\n", a + b);
    } else {
        printf("Invalid input.\n");
    }
    
    return 0;
}
